import Foundation

/// Replace with real values from Supabase Dashboard → API (or copy from `AppSecrets.example.swift`).
enum AppSecrets {
  static let supabaseURL = URL(string: "https://YOUR_PROJECT_REF.supabase.co")!
  static let supabaseAnonKey = "YOUR_SUPABASE_ANON_KEY"
}
